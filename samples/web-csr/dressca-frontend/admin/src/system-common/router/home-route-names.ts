/**
 * ホーム画面のルート名です。
 * 画面遷移では文字列リテラルではなくこの定数を参照してください。
 * system-common 外からは `@/system-common/router/route-names` 経由で参照します。
 */
export const homeRouteNames = {
  /** ホーム画面。 */
  home: 'home',
} as const
